#include <stdio.h>
#define pi 3.141592653
 main (void) {
 	fliat area, radius;
 	printf("enter your radius");
 	scandf("%f", &radius);
 	Area = PI*radius*radius;
 	printf("Your area is %.3f", area);

}
